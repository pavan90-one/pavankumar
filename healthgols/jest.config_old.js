// jest.config.js
module.exports = {
  testEnvironment: 'node', // Use the Node.js environment for testing
  testMatch: ['**/__tests__/**/*.js', '**/?(*.)+(spec|test).js'], // Define patterns for test files
  // You can add more configurations here, like coverage thresholds, module file extensions, etc.
};